# Level 1 — modal POD algebra

This test compiles the production `SPOPOD.f90` with the production SVD
backend. It verifies volume orthonormality, full-rank reconstruction,
projection coefficients, numerical rank, and the rule that rank is the number
of axial modal unknowns.

Run:

```sh
sh validation/level1/run_level1.sh
```

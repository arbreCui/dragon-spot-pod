# One direct Picard continuation from w

Date: 2026-08-16

Status: `PREPARED_NOT_RUN`.

This default-off stage permits exactly one evaluation

\[
x=G_2(w),\qquad w=G_2(v).
\]

The complete returned AX and snapshot archives of $w$ are the sole parent.
The unchanged host uses the fixed rank-two POD basis, performs three online
radial fixed-source transport solves, and then performs one axial solve.

The physical equations, normalization, rank, basis, strict inner terminals
and original stopping rule remain unchanged:

\[
R_\rho\leq5\times10^{-7},\qquad
R_L\leq5\times10^{-7},\qquad
R_a\leq5\times10^{-7}.
\]

The decision is their AND gate. $D_L$ and balances remain diagnostics. The
120/420 second bounds are external process limits and do not enter the map.
There is no proposal, coefficient, retry, relaxation, damping, clipping,
fitted closure, regularization, fallback or automatic successor.

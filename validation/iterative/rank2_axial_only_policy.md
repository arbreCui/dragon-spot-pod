# Rank-2 axial-only operational attempt

This is a second, separately authorized operational attempt at the axial half
of the same mathematical map $G_2(C_2(\Phi_7))$. It does not alter or
retroactively complete the first attempt, whose classification remains
`INVALID_MAP`.

The hash-locked radial response, fixed system and re-encoded parent are copied
to a new private directory. No radial equation is solved. Exactly one Dragon
process runs the unchanged rank-2 axial deck from its beginning with a
420-second host wall bound and no retry. This wall bound is an operational
resource limit; it is not supplied to the transport equations or convergence
test. The numerical solver/stopping tolerance remains $5\times10^{-7}$.

A map is valid only after normal Dragon termination, the predeclared strict
terminal records, finite physical state checks, unchanged input/runtime hashes
and the independent `--reencoded` Ganlib audit. A valid map is classified by
the existing rank-2 rule as `TOLERANCE_MET` or `VALID_NOT_MET`. Any failed gate
is `INVALID_MAP`, and no scientific result is published.

$D_L$, balance/source quantities and CPU/wall time remain diagnostics; their
magnitudes are not acceptance thresholds or physical-accuracy evidence.

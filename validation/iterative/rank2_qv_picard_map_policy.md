# One direct Picard map from the latest returned state v

Date: 2026-08-16

Status: `PREPARED_NOT_RUN`.

This default-off stage permits exactly one evaluation

\[
w=G_2(v), \qquad v=G_2(Q(s)).
\]

The complete hash-locked returned AX and snapshot archives of \(v\) are the
sole parent. The unchanged host uses the fixed rank-two POD basis, performs
three online radial fixed-source transport solves, and then performs one
axial solve. No proposal or affine coefficient enters this map.

The physical decks, normalization, rank, basis, inner solve terminals and
the original stopping rule are unchanged:

\[
R_\rho\leq5\times10^{-7},\qquad
R_L\leq5\times10^{-7},\qquad
R_a\leq5\times10^{-7}.
\]

The decision is their AND gate. The dimensional leakage change and balance
quantities remain diagnostics. The 120-second radial and 420-second axial
bounds are external process limits; they do not enter the equations or the
convergence decision.

One activation permits one attempt. There is no retry, relaxation, damping,
clipping, fitted closure, regularization, pseudoinverse, fallback, empirical
coefficient or automatic successor. Only `INVALID_MAP`, `TOLERANCE_MET` and
`VALID_NOT_MET` are allowed. This map alone cannot establish contraction,
convergence order, rank adequacy or physical accuracy.
